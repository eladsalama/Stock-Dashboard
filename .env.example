# =============================================================================
# Stock Dashboard Environment Configuration
# =============================================================================
# Copy this file to .env in the root directory:
#   cp .env.example .env  (Mac/Linux)
#   Copy-Item .env.example .env  (Windows PowerShell)
# =============================================================================

# --- Application Settings ---
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug
CORS_ORIGIN=*

# --- Database, Redis Cache (REQUIRED) ---
DATABASE_URL=postgres://postgres:postgres@localhost:5432/stockdash
REDIS_URL=redis://localhost:6379

# --- Google OAuth (Optional - for authentication features) ---
# Get your credentials from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID and set authorized origins to http://localhost:3100
# GOOGLE_CLIENT_ID=your_google_client_id_here